
<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %> 
  <% unless params[:artist_id] %> 
  <%= artist_select(Artist.all) %>
  <% end %>
  <br />

  <%= f.submit %>
<% end %>
